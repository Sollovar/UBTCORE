{
  "name": "UNBOUND-price-worker",
  "version": "1.0.0",
  "description": "Syncs live price + 24h change for all indexed pairs from GeckoTerminal into the local Postgres database.",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "dotenv": "^17.4.0",
    "node-cron": "^4.0.0",
    "pg": "^8.2.0"
  }
}
